import { Router, Request, Response } from 'express';import { z } from 'zod';import mongoose from 'mongoose';import crypto from 'crypto';import { requireSuperAdmin } from '../auth/middleware';import { TenantModel } from '../db/models/Tenant';import { DomainModel } from '../db/models/Domain';import { AuditLogModel } from '../db/models/AuditLog';import { ActivationTokenModel } from '../db/models/ActivationToken';import { emailService } from '../services/email.service';import { config } from '../config';import { getDefaultPlanSeatCount } from '../services/plan.service';import { isRegistrationEmailBlocked, REGISTRATION_EMAIL_BLOCKED_RESPONSE } from '../services/registration-block.service';import { stalwartClient } from '../stalwart/client';export const superAdminRouter = Router();// All routes in this router require Super Admin authenticationsuperAdminRouter.use(requireSuperAdmin);// ==========================================// DOMAIN DELETION REQUESTS (SUPER ADMIN QUEUE)// ==========================================